import 'package:flutter/material.dart';

class SolarAlarmScreen extends StatelessWidget {
  const SolarAlarmScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Solar Alarm')),
      body: const Center(child: Text('Solar Alarm Screen (placeholder)')),
    );
  }
}
